# 🌍 地球探索者 - 地理教学平台

## 使用方法

### 方法一：双击启动（推荐）

1. **解压压缩包**到任意文件夹
2. **双击 `启动网页.bat`** 文件
3. 等待浏览器自动打开，即可使用

> 💡 脚本会自动检测你的电脑是否安装了 Python 或 Node.js，选择最合适的服务器启动方式。

---

### 方法二：手动启动（如果脚本无法运行）

#### 使用 Python
```bash
# 进入项目目录
cd 地球探索者

# 启动服务器
python -m http.server 8000

# 打开浏览器访问
# http://localhost:8000/src/earth.html
```

#### 使用 Node.js
```bash
# 进入项目目录
cd 地球探索者

# 启动服务器（需要先安装 http-server）
npx http-server -p 8000 -c-1

# 打开浏览器访问
# http://localhost:8000/src/earth.html
```

---

## 🌐 访问地址

启动服务器后，在浏览器中打开：

```
http://localhost:8000/src/earth.html
```

---

## ❓ 常见问题

### Q: 双击 bat 文件闪退？
A: 说明你的电脑没有安装 Python 或 Node.js，请先安装其中一个。

### Q: 打开后提示"无法访问"？
A: 请确认服务器是否正在运行，端口 8000 是否被占用。

### Q: 手机能访问吗？
A: 可以！手机和电脑连同一个 WiFi，访问：
```
http://电脑IP:8000/src/earth.html
```
（电脑 IP 可在命令行输入 `ipconfig` 查看）

---

## 📁 项目结构

```
地球探索者/
├── 启动网页.bat          # 一键启动脚本
├── index.html            # 入口文件（自动跳转到 earth.html）
├── src/
│   ├── earth.html        # 主页面
│   ├── earth-optimization.js
│   └── lib/
│       ├── three.module.js
│       └── controls/
│           └── TrackballControls.js
└── assets/
    └── images/
```

---

## 🎯 功能介绍

- 🌍 3D 地球可视化
- 📏 测量工具（距离、面积）
- 🔭 天体观测（太阳系）
- 🌙 月相、日食演示
- 🗺️ 地理教学动画
- 🔊 语音交互（需配置 API）
